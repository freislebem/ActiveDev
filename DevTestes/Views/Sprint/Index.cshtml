@model IEnumerable<DevTestes.Models.Sprint>

@{
    ViewData["Title"] = "Index";
}

<h1>Sprint (Delphi / C#)</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeSprint)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeSprint)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SprintId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SprintId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SprintId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
